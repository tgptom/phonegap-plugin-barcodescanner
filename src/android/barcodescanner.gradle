repositories{
    flatDir{
        dirs 'libs'
    }
}

dependencies {
    implementation (name:'barcodescanner-release', ext:'aar')
}

android {
    packaging {
        resources {
            excludes += ['META-INF/NOTICE', 'META-INF/LICENSE']
        }
    }
}
